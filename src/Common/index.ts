import * as Filters from './Filters'
import * as Gamepad from './Gamepad'
import * as Keyboard from './Keyboard'
import * as Matchers from './Matchers'
import * as SimpleMatcher from './SimpleMatcher'
import * as StandardButtons from './StandardButtons'

const pkg = {
    Filters,
    Gamepad,
    Keyboard,
    Matchers,
    SimpleMatcher,
    StandardButtons,
}

export default pkg